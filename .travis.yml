language: python
python:
    - "2.7"

# Cache PlatformIO packages using Travis CI container-based infrastructure
sudo: false
cache:
    directories:
        - "~/.platformio"

env:
    - PLATFORMIO_CI_SRC=EMTB_RX/src
    - PLATFORMIO_CI_SRC=libraries
    
install:
    - pip install -U platformio

script:
    - platformio ci --board=pro8MHzatmega328

notifications:
  email:
    on_success: never
    on_failure: never
